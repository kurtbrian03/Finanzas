# agent.yaml — configuración base del agente declarativo PINPON
name: pinpon-agent
description: Agente declarativo para análisis financiero y operativo PINPON
version: "1.0.0"
instructions: knowledge/instructions.md
knowledge:
  - knowledge
actions:
  - actions
